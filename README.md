# ds-algo-python

A repository containing code implementations of all the popular data structures and algorithms. Current implementations include Python only.

There is a plan to introduce implementations in Java in the future.
